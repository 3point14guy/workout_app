<div class="row search-results">
  <% @athletes.each do |athlete| %>
    <div class="col-md-8 well member">
      <%= athlete.full_name %>
    </div>
  <% end %>
</div>

<%= will_paginate @athletes, renderer: WillPaginate::ActionView::BootstrapLinkRenderer %>
